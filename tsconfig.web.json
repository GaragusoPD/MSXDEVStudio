{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "preserve",
    "strict": true,
    "composite": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "baseUrl": ".",
    "paths": {
      "@renderer/*": ["src/renderer/src/*"],
      "@shared/*": ["src/shared/*"],
      "@monaco/*": ["node_modules/monaco-editor/esm/vs/*"]
    },
    "types": ["vite/client"]
  },
  "include": [
    "src/renderer/src/env.d.ts",
    "src/renderer/src/**/*",
    "src/renderer/src/**/*.vue",
    "src/preload/index.d.ts",
    "src/shared/**/*"
  ],
  // Shared tests run in Vitest under Node and are typechecked by
  // tsconfig.node.json. Leaving them in here as well would additionally require
  // them to be browser-safe, which is a constraint on the tests, not the code.
  "exclude": ["src/shared/**/*.test.ts"]
}
